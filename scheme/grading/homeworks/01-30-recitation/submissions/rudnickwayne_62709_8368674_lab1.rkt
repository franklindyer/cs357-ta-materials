
;; contains s1 s2 will return #t if s1 is contained in s2
;; return false otherwise
(define contains
    (lambda (l1 l2)
        (if (null? s1) #t #f))
    )
)

;; Testing equality of sets
;; (set-equals '(2 3 4) '(2 3 5)) => #f
(define set-equals
    (lambda (s1 s2)
        (if (s1 s2) set-equals)
    )
)
