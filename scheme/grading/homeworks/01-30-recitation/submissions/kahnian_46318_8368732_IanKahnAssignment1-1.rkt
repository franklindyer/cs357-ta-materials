
;; recitation assignment 1-30
;; Ian Kahn

;; contains?
;; (contains? s1 s2 should give #t if s1 is contained in s2 and
;;  #f if s1 is not contained in s2)

(define contains?
  (
  )  
    
)

;; checks if two sets are the same,, have the same elements
;; 2. set-equals?
(define set-equals?
	(
	)
)
;; ran out of time here unfortunately to work on these
;; in my compiler